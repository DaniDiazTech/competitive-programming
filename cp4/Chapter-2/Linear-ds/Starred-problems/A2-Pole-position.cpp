#include <bits/stdc++.h>
using namespace std;
int main(){
  #if LOCAL
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "l", stdout);
  #endif
  int n;
  while (cin >> n && n != 0){
    int a[n + 1];
    for (int i =1; i <= n; i++){
      int c, p; cin >> c >> p;
      
    }
  }
}