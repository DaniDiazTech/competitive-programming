#include <bits/stdc++.h>

using namespace std;

// T = text
// P = pattern
// 
// Build an occurence table of the characters of the pattern
// Only with the characters most to the right
// R = table

// x (pattern), m (size), R (table)
void precomputeR(string x, int m, int R[]){
  
}

int main(){

  return 0;
}